我需要开放一个编辑器，帮我澄清需求，大致是需要类似war3地图编辑器类似的可视化脚本编辑器，简单介绍就是，通过一行行的文本块来组合代码，例子
# 数据
## node config
{id:string,params:[{name:string,type:@string}],default:"字符串"},
{id:number,params:[{name:string,type:@number}],default:0},
{id:boolean,params:[{name:string,type:@boolean}],default:true},
{id:node_list,params:[{name:string,type:@node_list}],default:[]},

### node data
{
    "id": "1",
    "type": "node_list",
    "params": []
}


# 例子节点
## config
{id:select_area_unit_dot,kind:动作，params:[number,node_list],default:[100,[]], templet:["选取$1范围内的单位做动作","$2"]},
{id:node_list,kind:值，intent:1, params:[],default:[],templet:["$insert']},

## data
{
    "id": "1",
    "type": "select_area_unit_dot",
    "params": [
        {
            "id": "2",
            "type": "number",
            "value": 100
        }，
        {
            "id": "3",
            "type": "node_list",
            "value": []
        }
    ]
}


## 渲染templet流程
### 1.获取节点data
### 2.获取节点config
### 3.获取渲染器（超链接文本渲染器）将templet中的参数替换成node data的params，如果对应index的params没有，则创建个空白对应类型的node并赋值default value，
### 3.1 继续渲染子params的node data
### 3.3 $insert 特殊处理，如果node data的param不为空则显示--------插入---------，否则渲染parans的node data

## ui
<a href="node_id1"> 选取<a href="node_id2"> 100 </a>范围内的单位做动作
    <a href="node_id3">------点击插入--------</a>
</a>
-------------------------------------------------------------------------------------
|  动作                                                                              |
|  -选取  100 范围内的单位做动作                                                      |
|  --  ------点击插入--------                                                         |
|                                                                                    |
|                                                                                    |
|                                                                                    |
|                                                                                    |
|                                                                                    |
|                                                                                    |
|                                                                                    |
|                                                                                    |
|                                                                                    |
|                                                                                    |
|                                                                                    |
|                                                                                    |
-------------------------------------------------------------------------------------


# 点击流程
## 1.点击超链接
## 1.1，点击的是node的非参数部分，则编辑整个node data，否则编辑对应param
## 2.获取此节点对应node data以及对应的param index
## 3.弹出节点参数编辑器
## 


# 节点参数编辑器
## 传入参数，node_data, param_index
------------------------------
| a当前编辑节点kind           |
|  下拉框 （默认选中当前）     |
| b选中kind的节点列表          |
|  下拉框（默认选中当前）       |
|                             |
|                             |
|  c选中节点的渲染             |
|                             |
|                             |
|                             |
|                             |
|                             |
|                             |
-------------------------------
## a当前编辑节点kind，如果是目标node_data的param_index对应的params为空（通过insert进入）则选中动作kind，否则选中对应node_data的kind 
## b选中kind的过滤节点列表，通过下拉框选中选中对应prama_node_data
## c选中节点的渲染,如果有子节点也要一遍输出

# 缩进
## 渲染的节点如果是list，则需要缩进，如果是父节点有缩进则需要递增

# data
## node_data数据只有数据, 方便序列化